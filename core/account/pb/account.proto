syntax = "proto3";
package accountpb;

message Account{
    KeyPair keyPair = 1;
    repeated KeyPair subKeys = 2;
}

message KeyPair{
    bytes privateKey = 1;
    bytes publicKey = 2;
}

message Address{
    string address = 1;
}

message AccountConfig{
    string file_path = 1;
}
